# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0

# Optional: GPU support (NVIDIA)
# cupy-cuda11x>=10.0.0  # Uncomment for GPU support, adjust cuda version

# Optional: MPI support for parallel runs
# mpi4py>=3.1.0  # Uncomment for MPI support

# Jupyter support
jupyter>=1.0.0
ipykernel>=6.0.0

# Development tools (optional)
pytest>=7.0.0
black>=22.0.0
